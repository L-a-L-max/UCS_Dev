server:
  port: 8080

# ── T-22: Virtual Threads ──
spring:
  threads:
    virtual:
      enabled: ${VIRTUAL_THREADS_ENABLED:true}
  application:
    name: ucs-api-gateway
  cloud:
    gateway:
      routes:
        # ---- 无人机状态服务 ----
        - id: drone-state
          uri: ${DRONE_STATE_URI:http://localhost:8085}
          predicates:
            - Path=/api/v1/drones/**

        # ---- 指令下发服务 ----
        - id: command
          uri: ${COMMAND_URI:http://localhost:8084}
          predicates:
            - Path=/api/v1/commands/**

        # ---- 业务管理服务 ----
        - id: business
          uri: ${BUSINESS_URI:http://localhost:8086}
          predicates:
            - Path=/api/v1/users/**,/api/v1/user/**,/api/v1/auth/**,/api/v1/teams/**,/api/v1/events/**,/api/v1/tasks/**,/api/v1/rally-points/**,/api/v1/screen/**,/api/v1/pilot/**,/api/v1/leader/**,/api/v1/commander/**,/api/v1/operations/**,/api/v1/public/**,/api/v1/map/**,/api/v1/control/**

        # ---- WebSocket (路由到ucs-business，所有WebSocket广播/消费者均在此服务) ----
        - id: business-ws
          uri: ${BUSINESS_WS_URI:ws://localhost:8086}
          predicates:
            - Path=/ws/**

        # ---- DDS Gateway 遥测接入 (HTTP降级通道) ----
        - id: dds-gateway-ingest
          uri: ${INGEST_URI:http://localhost:8081}
          predicates:
            - Path=/api/v1/dds-gateway/telemetry,/api/v1/dds-gateway/command-ack

        # ---- 遥测存储服务 (历史查询) ----
        - id: telemetry-store
          uri: ${STORE_URI:http://localhost:8082}
          predicates:
            - Path=/api/v1/telemetry/**

      default-filters:
        - DedupeResponseHeader=Access-Control-Allow-Credentials Access-Control-Allow-Origin

  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}

jwt:
  secret: ${JWT_SECRET:change-me-in-production}

# ── T-43: Actuator + Micrometer ──
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus,gateway
  endpoint:
    health:
      show-details: always
      probes:
        enabled: true
  prometheus:
    metrics:
      export:
        enabled: true
  tracing:
    sampling:
      probability: ${TRACING_SAMPLE_RATE:0.1}
  otlp:
    tracing:
      endpoint: ${OTEL_EXPORTER_OTLP_ENDPOINT:http://localhost:4317}

# ── T-47: Rate Limiting ──
ucs:
  rate-limit:
    default-qps: ${RATE_LIMIT_DEFAULT_QPS:100}
    command-qps: ${RATE_LIMIT_COMMAND_QPS:10}

# ── T-69: Nacos Service Discovery (activate with spring.profiles.active=nacos) ──
# spring:
#   cloud:
#     nacos:
#       discovery:
#         server-addr: ${NACOS_SERVER_ADDR:localhost:8848}
#         namespace: ${NACOS_NAMESPACE:public}
#         group: ${NACOS_GROUP:DEFAULT_GROUP}

# ── T-56: TLS 加密配置（生产环境启用，去掉注释即可） ──
# spring:
#   kafka:
#     ssl:
#       key-store-location: classpath:kafka-keystore.jks
#       key-store-password: ${KAFKA_SSL_KEYSTORE_PASS:changeme}
#       trust-store-location: classpath:kafka-truststore.jks
#       trust-store-password: ${KAFKA_SSL_TRUSTSTORE_PASS:changeme}
#       key-store-type: JKS
#     properties:
#       security.protocol: SSL
#   data:
#     redis:
#       ssl:
#         enabled: true
#       lettuce:
#         client-options:
#           ssl-options:
#             trust-store-type: PEM
#             trust-store-path: ${REDIS_CA_CERT_PATH:/etc/tls/ca-cert.pem}
