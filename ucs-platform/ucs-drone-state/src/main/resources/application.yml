server:
  port: 8085

spring:
  application:
    name: ucs-drone-state
  data:
    redis:
      cluster:
        nodes: ${REDIS_CLUSTER_NODES:localhost:6371,localhost:6372,localhost:6373,localhost:6374,localhost:6375,localhost:6376}
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      lettuce:
        pool:
          max-active: 32
          max-idle: 16
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:ucs}
    username: ${DB_USER:ucs}
    password: ${DB_PASS:ucs_password}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: false

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
