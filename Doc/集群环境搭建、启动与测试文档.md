# UCS 集群环境搭建、启动与测试文档

> 版本：v2.1 — 对应 `ClusterArchitectureUpgrade` 分支（T-01 ~ T-74 + Phase 14）
> 最后更新：2026-04

---

## 目录

1. [环境总览与硬件要求](#1-环境总览与硬件要求)
2. [软件依赖清单](#2-软件依赖清单)
3. [目录结构说明](#3-目录结构说明)
4. [环境变量参考表](#4-环境变量参考表)
5. [第一步：基础设施启动（Kafka + Redis + PostgreSQL）](#5-第一步基础设施启动kafka--redis--postgresql)
6. [第二步：监控与可观测性启动](#6-第二步监控与可观测性启动)
7. [第三步：后端服务启动](#7-第三步后端服务启动)
8. [第四步：网关启动（DDS / MAVLink）](#8-第四步网关启动dds--mavlink)
9. [第五步：前端启动](#9-第五步前端启动)
10. [K8s 部署（生产环境）](#10-k8s-部署生产环境)
11. [TLS 加密配置（生产环境）](#11-tls-加密配置生产环境)
12. [Nacos 配置中心集成](#12-nacos-配置中心集成)
13. [健康检查与验证清单](#13-健康检查与验证清单)
14. [监控仪表盘访问指南](#14-监控仪表盘访问指南)
15. [常见问题排查](#15-常见问题排查)
16. [安全配置（JWT RSA / 限流 / 幂等）](#16-安全配置jwt-rsa--限流--幂等)
17. [性能测试指标参考](#17-性能测试指标参考)
18. [Redis Sentinel 集群（自动故障转移）](#18-redis-sentinel-集群自动故障转移)
19. [Docker 多实例部署验证（Pre-K8S）](#19-docker-多实例部署验证pre-k8s)
20. [一键启动脚本](#20-一键启动脚本)
21. [10问Q&A实施清单](#21-10问qa实施清单)

---

## 1. 环境总览与硬件要求

### 架构拓扑（微服务架构）

```
┌─────────────┐   ┌─────────────┐   ┌─────────────┐
│ DDS Gateway │   │  MAVLink GW │   │  Frontend   │
│  (Python)   │   │  (Python)   │   │  (React)    │
└──────┬──────┘   └──────┬──────┘   └──────┬──────┘
       │                 │                 │ WebSocket/STOMP
       ▼                 ▼                 ▼
┌──────────────────────────────────────────────────┐
│              Kafka 3-Broker KRaft                │
│        kafka-1:9092  kafka-2:9093  kafka-3:9094  │
└───┬──────────┬──────────┬──────────┬─────────────┘
    │          │          │          │
    ▼          ▼          ▼          ▼
┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐
│ingest  │ │store   │ │push    │ │command │
│ :8081  │ │ :8082  │ │ :8083  │ │ :8084  │
└────────┘ └────────┘ └────────┘ └────────┘
                                      ↑
┌────────────┐  ┌────────────┐        │
│api-gateway │→ │ business   │────────┘
│  :8080     │  │  :8086     │  ┌────────────┐
└────────────┘  └────────────┘  │drone-state │
       ↑                        │  :8085     │
   [Ingress]                    └────────────┘
       │             │              │
       ▼             ▼              ▼
┌─────────┐   ┌──────────────┐   ┌─────────────┐
│PostgreSQL│   │ Redis Cluster│   │ TimescaleDB │
│  :5432   │   │ 6节点(3M+3S) │   │   :5433     │
└─────────┘   └──────────────┘   └─────────────┘
```

### 硬件推荐

| 环境   | CPU    | 内存   | 磁盘    | 说明                         |
|--------|--------|--------|---------|------------------------------|
| 开发   | 4 Core | 16 GB  | 50 GB   | 单机 Docker Compose 部署     |
| 测试   | 8 Core | 32 GB  | 100 GB  | 可运行500架无人机仿真        |
| 生产   | 16+ Core| 64+ GB| 500+ GB | K8s 多节点，千架无人机并发   |

---

## 2. 软件依赖清单

| 软件             | 最低版本      | 用途                         |
|------------------|---------------|------------------------------|
| Docker           | 24.0+         | 容器运行时                   |
| Docker Compose   | 2.20+         | 多容器编排                   |
| JDK              | 21            | 后端运行时（虚拟线程）       |
| Maven            | 3.9+          | 后端构建                     |
| Node.js          | 18+           | 前端构建                     |
| npm / yarn       | 9+ / 1.22+    | 前端依赖管理                 |
| Python           | 3.10+         | DDS/MAVLink 网关             |
| kubectl          | 1.28+         | K8s 部署（生产环境）         |
| openssl + keytool| -             | TLS 证书生成                 |

---

## 3. 目录结构说明

```
UCS_Dev/
├── ucs-platform/                         # 微服务架构后端（8模块）
│   ├── pom.xml                           # 父POM（Spring Boot 3.2.0, Spring Cloud 2023.0.0）
│   ├── ucs-common/                       # 共享库（DTO、Entity、工具类）
│   │   └── src/main/java/com/ucs/common/
│   │       ├── config/GlobalExceptionHandler.java  # T-48: 全局异常处理
│   │       └── dto/
│   │           ├── ApiResponse.java                # T-55: 统一响应包装
│   │           └── ErrorCode.java                  # T-55: 错误码枚举
│   ├── ucs-api-gateway/                  # API网关（Spring Cloud Gateway, WebFlux）
│   │   └── src/main/java/com/ucs/gateway/filter/
│   │       ├── JwtAuthGatewayFilter.java           # T-46: JWT RSA验证
│   │       └── RateLimitFilter.java                # T-47: 限流过滤器
│   ├── ucs-telemetry-ingest/             # 遥测接入（Kafka Consumer）
│   │   └── src/main/java/com/ucs/ingest/config/
│   │       └── TelemetryMetrics.java               # T-44: Micrometer性能埋点
│   ├── ucs-telemetry-store/              # 遥测存储（Kafka→TimescaleDB）
│   ├── ucs-realtime-push/                # 实时推送（Kafka→WebSocket STOMP）
│   ├── ucs-command/                      # 命令调度（REST→Kafka Producer）
│   │   └── src/main/java/com/ucs/command/service/
│   │       └── IdempotencyService.java             # T-50: Redis SETNX幂等
│   ├── ucs-drone-state/                  # 无人机状态缓存（Redis + REST）
│   └── ucs-business/                     # 业务逻辑（用户/团队/任务/控制）
│       └── src/main/java/com/ucs/business/
│           ├── config/ShedLockConfig.java           # T-45: 分布式定时任务锁
│           └── security/JwtUtil.java                # T-46: RSA+HMAC双模JWT
│   # 每个模块均包含:
│   #   src/main/resources/application.yml           # T-43: Actuator+Prometheus配置
│   #   src/main/resources/bootstrap.yml             # T-53: Nacos引导配置
│   #   src/main/resources/logback-spring.xml        # T-52: 结构化JSON日志
├── backend/                              # 旧单体后端（保留兼容，不再新增功能）
├── dds-gateway/                          # DDS 网关（仿真无人机）
├── mavlink-gateway/                      # MAVLink 网关（真实无人机）
├── frontend/ucs-dashboard/               # React 前端
├── k8s/                                  # T-41: K8s 部署清单
│   ├── namespace.yaml
│   ├── configmap.yaml                    # 公共ConfigMap（Kafka/Redis/DB地址）
│   ├── secrets.yaml
│   ├── microservices-deployment.yaml     # 7个微服务Deployment+Service+HPA
│   └── ingress.yaml                      # 统一入口→ucs-api-gateway
├── monitoring/                           # T-42: 监控配置
│   ├── prometheus.yml
│   ├── alert-rules.yml
│   ├── alertmanager.yml
│   └── grafana/provisioning/
│       ├── datasources/prometheus.yml
│       └── dashboards/json/ucs-overview.json
├── scripts/tls/                          # T-56: TLS 证书生成脚本
│   └── generate-certs.sh
├── docker-compose.yml                    # PostgreSQL + TimescaleDB
├── docker-compose-kafka.yml              # Kafka 3-Broker KRaft
├── docker-compose-redis-cluster.yml      # Redis 6节点集群
├── docker-compose-monitoring.yml         # Prometheus+Grafana+Jaeger+Nacos
└── Doc/                                  # 文档目录
```

---

## 4. 环境变量参考表

### 微服务公共环境变量

| 变量名                       | 默认值                                      | 说明                                |
|------------------------------|---------------------------------------------|-------------------------------------|
| `DB_USERNAME`                | `ucs_user`                                   | PostgreSQL 用户名                   |
| `DB_PASSWORD`                | `ucs_password`                               | PostgreSQL 密码                     |
| `JWT_SECRET`                 | `change-me-in-production`                    | JWT HMAC 签名密钥                   |
| `KAFKA_BOOTSTRAP`           | `kafka-1:29092,kafka-2:29092,kafka-3:29092`  | Kafka Broker 地址列表               |
| `REDIS_HOST`                 | `localhost`                                  | Redis 地址                          |
| `REDIS_PORT`                 | `6379`                                       | Redis 端口                          |
| `NACOS_SERVER_ADDR`          | `localhost:8848`                             | Nacos 配置中心地址                  |
| `NACOS_NAMESPACE`            | `ucs-dev`                                    | Nacos 命名空间                      |

### 可选环境变量

| 变量名                       | 默认值          | 说明                                  |
|------------------------------|-----------------|---------------------------------------|
| `jwt.rsa.private-key`        | *(空)*          | T-46: RSA 私钥 Base64（启用RSA模式）  |
| `jwt.rsa.public-key`         | *(空)*          | T-46: RSA 公钥 Base64                 |
| `TRACING_SAMPLE_RATE`        | `0.1`           | T-51: 追踪采样率（0.0~1.0）          |
| `OTEL_EXPORTER_OTLP_ENDPOINT`| `http://localhost:4317` | T-51: Jaeger OTLP 端点       |
| `NACOS_ENABLED`              | `false`         | T-53: 是否启用 Nacos 配置中心        |
| `NACOS_SERVER_ADDR`          | `localhost:8848`| T-53: Nacos 服务地址                  |
| `NACOS_NAMESPACE`            | `ucs`           | T-53: Nacos 命名空间                  |

### DDS 网关环境变量

| 变量名                       | 默认值          | 说明                                  |
|------------------------------|-----------------|---------------------------------------|
| `KAFKA_BOOTSTRAP_SERVERS`    | `localhost:9092` | Kafka Broker 地址                    |
| `REDIS_HOST`                 | `localhost`      | Redis 地址                           |
| `REDIS_PORT`                 | `6379`           | Redis 端口                           |
| `DDS_API_KEY`                | -                | 网关认证 API Key                     |
| `BACKEND_URL`                | `http://localhost:8080` | 后端 API 地址                 |

### MAVLink 网关环境变量

| 变量名                       | 默认值          | 说明                                  |
|------------------------------|-----------------|---------------------------------------|
| `MAVLINK_LISTEN_PORT`        | `14550`          | MAVLink UDP 监听端口                 |
| `KAFKA_BOOTSTRAP_SERVERS`    | `localhost:9092` | Kafka Broker 地址                    |

---

## 5. 第一步：基础设施启动（Kafka + Redis + PostgreSQL）

### 5.1 创建 Docker 网络

```bash
docker network create ucs_dev_kafka-net 2>/dev/null || true
```

### 5.2 启动 Kafka 3-Broker KRaft 集群

```bash
docker-compose -f docker-compose-kafka.yml up -d
```

**验证：**
```bash
# 等待30秒让集群初始化
sleep 30

# 检查3个Broker是否正常
docker exec kafka-1 kafka-metadata.sh --snapshot /tmp/kraft-combined-logs/__cluster_metadata-0/00000000000000000000.log --cluster-id $(docker exec kafka-1 cat /tmp/kraft-combined-logs/__cluster_metadata-0/.cluster_id 2>/dev/null || echo "unknown") 2>/dev/null || echo "Kafka brokers starting..."

# 列出 Topic（可能为空，说明集群正常）
docker exec kafka-1 kafka-topics.sh --list --bootstrap-server kafka-1:29092
```

### 5.3 启动 Redis 6节点集群

```bash
docker-compose -f docker-compose-redis-cluster.yml up -d
```

**验证：**
```bash
# 等待集群就绪
sleep 10

# 检查集群状态
docker exec redis-node-1 redis-cli -p 6371 cluster info | head -5
# 预期: cluster_state:ok, cluster_slots_assigned:16384
```

### 5.4 启动 PostgreSQL + TimescaleDB

```bash
docker-compose up -d postgres timescaledb
```

**验证：**
```bash
docker exec -it postgres psql -U ucs_user -d ucsdb -c "SELECT version();"
```

### 5.5 数据库初始化

首次部署需要执行 Flyway 迁移，后端启动时自动执行。如需手动：

```bash
cd backend
mvn flyway:migrate -Dflyway.url=jdbc:postgresql://localhost:5432/ucsdb \
    -Dflyway.user=ucs_user -Dflyway.password=ucs_password
```

---

## 6. 第二步：监控与可观测性启动

```bash
docker-compose -f docker-compose-monitoring.yml up -d
```

此命令启动以下服务：

| 服务            | 端口  | 用途                              |
|-----------------|-------|-----------------------------------|
| Prometheus      | 9090  | 指标采集与告警规则                |
| Grafana         | 3001  | 可视化仪表盘（admin/ucs_admin）   |
| Alertmanager    | 9093  | 告警通知                          |
| Kafka Exporter  | 9308  | Kafka 集群指标导出                |
| Redis Exporter  | 9121  | Redis 集群指标导出                |
| Node Exporter   | 9100  | 主机 CPU/内存/磁盘指标            |
| Jaeger          | 16686 | 分布式追踪 UI（T-51）            |
| Nacos           | 8848  | 配置中心（T-53，可选）            |

**验证：**
```bash
# Prometheus 目标状态
curl -s http://localhost:9090/api/v1/targets | python3 -m json.tool | head -20

# Grafana 登录
open http://localhost:3001   # admin / ucs_admin

# Jaeger UI
open http://localhost:16686
```

---

## 7. 第三步：微服务后端启动（ucs-platform）

### 7.1 构建全部微服务

```bash
cd ucs-platform
mvn clean package -DskipTests
```

### 7.2 微服务端口分配

| 微服务                 | 端口  | 说明                              |
|------------------------|-------|-----------------------------------|
| ucs-api-gateway        | 8080  | API网关（外部入口，JWT验证+限流） |
| ucs-telemetry-ingest   | 8081  | 遥测接入（Kafka Consumer）        |
| ucs-telemetry-store    | 8082  | 遥测存储（Kafka→TimescaleDB）     |
| ucs-realtime-push      | 8083  | 实时推送（Kafka→WebSocket STOMP） |
| ucs-command            | 8084  | 命令调度（REST→Kafka Producer）   |
| ucs-drone-state        | 8085  | 无人机状态缓存（Redis + REST）    |
| ucs-business           | 8086  | 业务逻辑（用户/团队/任务/控制）   |

### 7.3 开发环境启动（逐个启动）

```bash
# 1. API网关（必须最先启动，负责路由和鉴权）
java -jar ucs-api-gateway/target/ucs-api-gateway-*.jar --spring.profiles.active=dev &

# 2. 业务服务（JWT签发、用户管理）
java -jar ucs-business/target/ucs-business-*.jar --spring.profiles.active=dev &

# 3. 遥测接入
java -jar ucs-telemetry-ingest/target/ucs-telemetry-ingest-*.jar --spring.profiles.active=dev &

# 4. 遥测存储
java -jar ucs-telemetry-store/target/ucs-telemetry-store-*.jar --spring.profiles.active=dev &

# 5. 实时推送
java -jar ucs-realtime-push/target/ucs-realtime-push-*.jar --spring.profiles.active=dev &

# 6. 命令调度
java -jar ucs-command/target/ucs-command-*.jar --spring.profiles.active=dev &

# 7. 无人机状态
java -jar ucs-drone-state/target/ucs-drone-state-*.jar --spring.profiles.active=dev &
```

### 7.4 多实例模拟（测试 ShedLock / WebSocket 多实例）

```bash
# ucs-business 实例2（端口8087）
java -jar ucs-business/target/ucs-business-*.jar --server.port=8087 --spring.profiles.active=prod &

# ucs-telemetry-ingest 实例2（端口8088）
java -jar ucs-telemetry-ingest/target/ucs-telemetry-ingest-*.jar --server.port=8088 --spring.profiles.active=prod &
```

> **注意：** 多实例模式下：
> - ShedLock（T-45）确保 ucs-business 定时任务只在一个实例执行
> - WebSocket 通过 Redis Pub/Sub（T-27）实现跨 ucs-realtime-push 实例消息分发
> - Kafka Consumer Group 自动在同一模块的多实例间分配 partition

### 7.5 验证各微服务健康

```bash
# 逐个检查所有微服务健康状态
for port in 8080 8081 8082 8083 8084 8085 8086; do
  echo "=== Port $port ==="
  curl -s http://localhost:$port/actuator/health | python3 -m json.tool 2>/dev/null || echo "FAILED"
done

# Prometheus 指标端点（以 ucs-telemetry-ingest 为例）
curl -s http://localhost:8081/actuator/prometheus | head -30

# Swagger API 文档（ucs-business）
open http://localhost:8086/swagger-ui.html
```

---

## 8. 第四步：网关启动（DDS / MAVLink）

### 8.1 DDS 网关（仿真无人机）

```bash
cd dds-gateway
pip install -r requirements.txt

# 启动
python dds_gateway.py
```

### 8.2 MAVLink 网关（真实无人机）

```bash
cd mavlink-gateway
pip install -r requirements.txt

# 启动（监听 UDP 14550）
python mavlink_gateway.py
```

> **注意：** DDS 和 MAVLink 网关可同时运行，通过 `uav_id` 前缀区分无人机来源，不会产生冲突。

---

## 9. 第五步：前端启动

```bash
cd frontend/ucs-dashboard
npm install
npm run dev
```

前端默认监听 `http://localhost:5173`。

**环境变量（.env 或 .env.local）：**
```env
VITE_API_BASE_URL=http://localhost:8080
VITE_WS_URL=ws://localhost:8080/ws
```

---

## 10. K8s 部署（生产环境）

### 10.1 前置条件

- K8s 集群（1.28+）已就绪
- kubectl 已配置 kubeconfig
- 镜像已推送到 Container Registry

### 10.2 部署顺序

```bash
# 1. 创建命名空间
kubectl apply -f k8s/namespace.yaml

# 2. 创建配置和密钥
kubectl apply -f k8s/configmap.yaml
kubectl apply -f k8s/secrets.yaml

# 3. 部署全部微服务（7个Deployment + Service + HPA）
kubectl apply -f k8s/microservices-deployment.yaml

# 4. 配置 Ingress（统一入口→ucs-api-gateway）
kubectl apply -f k8s/ingress.yaml
```

### 10.3 验证

```bash
# 查看所有微服务 Pod 状态
kubectl get pods -n ucs -o wide

# 查看 HPA 状态
kubectl get hpa -n ucs

# 查看 Service
kubectl get svc -n ucs

# 查看各微服务日志
kubectl logs -f deployment/ucs-api-gateway -n ucs
kubectl logs -f deployment/ucs-business -n ucs
kubectl logs -f deployment/ucs-telemetry-ingest -n ucs

# 端口转发网关测试
kubectl port-forward svc/ucs-api-gateway-service 8080:8080 -n ucs
```

### 10.4 K8s 微服务配置说明

| 微服务              | 副本数 | HPA范围 | CPU Req/Limit  | 内存 Req/Limit  |
|---------------------|--------|---------|----------------|------------------|
| ucs-api-gateway     | 2      | 2~6     | 250m / 1000m   | 256Mi / 768Mi    |
| ucs-telemetry-ingest| 3      | 3~10    | 500m / 2000m   | 512Mi / 1024Mi   |
| ucs-telemetry-store | 2      | 2~6     | 500m / 2000m   | 512Mi / 1024Mi   |
| ucs-realtime-push   | 2      | 2~8     | 250m / 1000m   | 256Mi / 768Mi    |
| ucs-command         | 2      | 2~6     | 250m / 1000m   | 256Mi / 768Mi    |
| ucs-drone-state     | 2      | -       | 250m / 1000m   | 256Mi / 768Mi    |
| ucs-business        | 3      | 3~10    | 500m / 2000m   | 512Mi / 1536Mi   |

所有微服务均配置：
- 健康检查: `/actuator/health`（liveness + readiness + startup probe）
- 滚动更新: maxSurge=1, maxUnavailable=0（零宕机部署）
- HPA 触发: CPU > 70%
- Prometheus 注解: 自动采集 `/actuator/prometheus`

---

## 11. TLS 加密配置（生产环境）

### 11.1 生成自签名证书

```bash
cd scripts/tls
chmod +x generate-certs.sh
./generate-certs.sh
```

生成的文件：

| 文件路径                           | 用途                    |
|------------------------------------|------------------------|
| `certs/ca/ca-cert.pem`            | CA 根证书               |
| `certs/kafka/kafka-keystore.jks`  | Kafka Broker 密钥库     |
| `certs/kafka/kafka-truststore.jks`| Kafka 信任库            |
| `certs/redis/redis-cert.pem`      | Redis TLS 证书          |
| `certs/redis/redis-key.pem`       | Redis TLS 私钥          |

### 11.2 启用 Kafka TLS

1. 将 `kafka-keystore.jks` 和 `kafka-truststore.jks` 复制到各微服务的 `src/main/resources/`
2. 在各模块 `application.yml` 中取消注释 TLS 配置：

```properties
spring.kafka.properties.security.protocol=SSL
spring.kafka.ssl.trust-store-location=classpath:kafka-truststore.jks
spring.kafka.ssl.trust-store-password=${KAFKA_SSL_TRUSTSTORE_PASS}
spring.kafka.ssl.key-store-location=classpath:kafka-keystore.jks
spring.kafka.ssl.key-store-password=${KAFKA_SSL_KEYSTORE_PASS}
```

### 11.3 启用 Redis TLS

在各模块 `application.yml` 中取消注释：

```properties
spring.data.redis.ssl.enabled=true
```

> **生产环境务必替换自签名证书为正式 CA 签发的证书。**

---

## 12. Nacos 配置中心（可选）

### 12.1 启用 Nacos

每个微服务均包含 `bootstrap.yml`，设置环境变量后启动即可自动连接 Nacos：

```bash
export NACOS_SERVER_ADDR=localhost:8848
export NACOS_NAMESPACE=ucs-dev

# 启动任意微服务（以 ucs-business 为例）
java -jar ucs-platform/ucs-business/target/ucs-business-*.jar
```

### 12.2 在 Nacos 中添加配置

访问 Nacos 管理界面：`http://localhost:8848/nacos`（默认 nacos/nacos）

创建以下配置项（Properties 格式）：

**Data ID: `common-redis.properties`**
```properties
spring.data.redis.cluster.nodes=redis-node-1:6371,redis-node-2:6372,redis-node-3:6373,redis-node-4:6374,redis-node-5:6375,redis-node-6:6376
spring.data.redis.cluster.max-redirects=3
```

**Data ID: `common-kafka.properties`**
```properties
spring.kafka.bootstrap-servers=kafka-1:29092,kafka-2:29092,kafka-3:29092
```

### 12.3 动态刷新

修改 Nacos 配置后，后端会自动拉取新配置（`@RefreshScope` 标注的 Bean 会重新初始化），无需重启服务。

---

## 13. 健康检查与验证清单

### 启动后逐项验证

| 序号 | 检查项                           | 命令 / URL                                     | 预期结果                |
|------|----------------------------------|-------------------------------------------------|------------------------|
| 1    | Kafka Broker 集群                | `docker exec kafka-1 kafka-topics.sh --list --bootstrap-server kafka-1:29092` | 无报错                  |
| 2    | Redis 集群状态                   | `docker exec redis-node-1 redis-cli -p 6371 cluster info` | `cluster_state:ok`      |
| 3    | PostgreSQL 连接                  | `curl localhost:8080/actuator/health`           | `db.status: UP`         |
| 4    | API网关健康                      | `curl localhost:8080/actuator/health`           | `status: UP`            |
| 4b   | 业务服务健康                      | `curl localhost:8086/actuator/health`           | `status: UP`            |
| 5    | Prometheus 指标                  | `curl localhost:8081/actuator/prometheus`        | 含 `ucs_telemetry_*`   |
| 6    | Prometheus 目标                  | `http://localhost:9090/targets`                 | 全部 UP                 |
| 7    | Grafana 仪表盘                   | `http://localhost:3001`                         | UCS Overview 可见       |
| 8    | Jaeger 追踪                      | `http://localhost:16686`                        | 有7个微服务可选      |
| 9    | Swagger API                      | `http://localhost:8086/swagger-ui.html`         | API 文档页面可访问      |
| 10   | 前端页面                         | `http://localhost:5173`                         | 登录页面正常显示        |
| 11   | WebSocket 连接                   | 前端打开后检查浏览器控制台                      | `STOMP connected`       |
| 12   | 网关遥测                         | 启动 DDS/MAVLink 网关后看前端地图               | 无人机位置显示          |

### ShedLock 验证（T-45）

启动两个后端实例后，观察日志中的定时任务执行情况：

```bash
# 实例1日志
grep "broadcastDroneSummary\|broadcastEvents\|reconcileDbRedis" instance1.log

# 实例2日志
grep "broadcastDroneSummary\|broadcastEvents\|reconcileDbRedis" instance2.log
```

预期：同一时刻只有一个实例执行定时任务，另一个实例因 ShedLock 跳过。

### 限流验证（T-47）

```bash
# 快速发送请求测试限流
for i in $(seq 1 120); do
  curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8080/api/control/command \
    -H "Content-Type: application/json" \
    -H "Authorization: Bearer <token>" \
    -d '{"uavId":"test","commandType":"HOLD"}' &
done
wait

# 预期：超过100QPS后返回429
```

### 幂等性验证（T-50）

```bash
# 连续发送相同命令
curl -X POST http://localhost:8080/api/control/command \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <token>" \
  -d '{"uavId":"uav_001","commandType":"HOLD","params":"{}"}'

# 5秒内再次发送相同命令
curl -X POST http://localhost:8080/api/control/command \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer <token>" \
  -d '{"uavId":"uav_001","commandType":"HOLD","params":"{}"}'

# 预期：第二次返回 "CMD_DEDUP" 标识
```

---

## 14. 监控仪表盘访问指南

### Grafana（http://localhost:3001）

- **用户名：** `admin`
- **密码：** `ucs_admin`
- **预置仪表盘：** `UCS IoT Overview`

仪表盘面板说明：

| 面板名称                          | 指标来源              | 含义                           |
|-----------------------------------|-----------------------|--------------------------------|
| Telemetry Processing Rate         | `ucs_telemetry_consumed_total` | 遥测消息消费速率 (msg/s)      |
| Telemetry Process Duration        | `ucs_telemetry_process_duration` | 消息处理耗时 P50/P95/P99      |
| Cache Hit Rate                    | `ucs_cache_hit/miss_total`     | Redis 缓存命中率              |
| Kafka Consumer Lag                | `kafka_consumergroup_lag_sum`  | Kafka 消费积压量              |
| Command Dispatch / Failed         | `ucs_command_dispatched_total` | 控制命令下发成功/失败率       |
| JVM Heap Memory                   | `jvm_memory_used_bytes`        | JVM 堆内存使用量              |
| HikariCP Active Connections       | `hikaricp_connections_active`  | 数据库连接池活跃连接          |
| System CPU Usage                  | `system_cpu_usage`             | 系统/JVM CPU 使用率           |
| Redis Memory / Ops                | `redis_memory_used_bytes`      | Redis 内存和命令吞吐          |
| WebSocket Push Rate               | `ucs_websocket_push_total`     | WebSocket 推送速率            |
| Circuit Breaker State             | `resilience4j_circuitbreaker_state` | 熔断器状态 (0=CLOSED, 1=OPEN) |
| Rate Limiter Rejections           | `http_server_requests{status=429}` | 被限流拒绝的请求数           |

### Prometheus（http://localhost:9090）

- **Targets 页面：** http://localhost:9090/targets — 查看所有采集目标状态
- **告警规则：** http://localhost:9090/rules — 查看告警规则触发情况

### Jaeger（http://localhost:16686）

- 选择 Service: `ucs-backend`
- 查看请求在 Kafka Consumer → Service → Repository → Redis 的完整链路

---

## 15. 常见问题排查

### Q1: Kafka 连接失败

**现象：** 后端日志 `Connection to node -1 could not be established`

**排查：**
```bash
# 检查 Kafka 容器是否运行
docker ps | grep kafka

# 检查网络连通性
docker exec backend ping kafka-1

# 检查 bootstrap-servers 配置
# Docker内部通信使用 kafka-1:29092（不是 localhost:9092）
```

**解决：** 确保 `KAFKA_BOOTSTRAP_SERVERS` 使用容器名 + 内部端口。

### Q2: Redis 集群连接失败

**现象：** `Unable to connect to Redis cluster`

**排查：**
```bash
# 检查 Redis 集群状态
docker exec redis-node-1 redis-cli -p 6371 cluster nodes

# 确认所有节点正常
docker exec redis-node-1 redis-cli -p 6371 cluster info | grep cluster_state
```

**解决：** 等待集群自动选主完成（通常30秒内）。

### Q3: ShedLock 未生效（定时任务重复执行）

**排查：**
```bash
# 检查 Redis 中 ShedLock 键
docker exec redis-node-1 redis-cli -p 6371 keys "shedlock:*"

# 检查是否引入了 shedlock 依赖
grep shedlock ucs-platform/ucs-business/pom.xml
```

**解决：** 确保 `@EnableSchedulerLock` 注解已添加且 Redis 连接正常。

### Q4: Prometheus 无法采集后端指标

**排查：**
```bash
# 确认 Actuator 端点可访问
curl http://localhost:8080/actuator/prometheus

# 检查 Prometheus 配置中的 target 地址
cat monitoring/prometheus.yml | grep targets
```

**解决：** 确保 Prometheus 容器能访问后端容器（同一 Docker 网络）。

### Q5: JWT RSA 模式未生效

**排查：**
```bash
# 确认环境变量已设置
echo $jwt.rsa.private-key
echo $jwt.rsa.public-key

# 如果为空，JWT 自动回退到 HMAC 模式（这是正常行为）
```

**生成 RSA 密钥对：**
```bash
# 生成 RSA 私钥
openssl genrsa -out private.pem 2048
# 提取公钥
openssl rsa -in private.pem -pubout -out public.pem
# Base64 编码（去掉头尾和换行）
cat private.pem | grep -v "BEGIN\|END" | tr -d '\n'
cat public.pem | grep -v "BEGIN\|END" | tr -d '\n'
```

### Q6: 前端 WebSocket 断连

**排查：**
- 检查浏览器控制台的 WebSocket 错误
- 确认后端 `/ws` 端点可访问
- 多实例模式下确认 Redis Broker 已启用

**解决：** 前端 StompConnectionManager 内置了自动重连机制（指数退避），通常等待即可自动恢复。

---

## 16. 安全配置（JWT RSA / 限流 / 幂等）

### T-46: JWT RSA 双模配置

| 模式   | 触发条件                                    | 适用场景         |
|--------|---------------------------------------------|------------------|
| RSA    | 设置 `jwt.rsa.private-key` 和 `public-key`  | 生产环境         |
| HMAC   | RSA 密钥为空（默认）                         | 开发/测试环境    |

### T-47: 限流配置

| 端点类别       | 限流阈值       | 配置项                     |
|----------------|----------------|----------------------------|
| 通用 API       | 100 QPS        | `ucs.rate-limit.default-qps` |
| 控制命令 API   | 10 QPS         | `ucs.rate-limit.command-qps`  |

超限返回 HTTP 429 + `ApiResponse{code:42900, message:"Too many requests"}`

### T-49: 熔断器配置

| 熔断器名称     | 滑动窗口 | 失败率阈值 | 半开等待时间 |
|----------------|----------|------------|-------------|
| redis          | 10       | 50%        | 30s         |
| gateway-http   | 5        | 60%        | 10s         |

### T-50: 幂等性配置

- **去重窗口：** 5秒
- **Key 格式：** `cmd:idempotent:{SHA256(uavId+commandType+params)}`
- **存储：** Redis SETNX + TTL

---

## 17. 性能测试指标参考

### 关键 Prometheus 指标用于性能基准

| 指标                                    | 告警阈值               | 含义                    |
|-----------------------------------------|------------------------|-------------------------|
| `ucs_telemetry_process_duration` P99    | > 100ms                | 遥测消息处理延迟        |
| `kafka_consumergroup_lag_sum`           | > 50,000 (Warning)     | Kafka 消费积压          |
|                                         | > 300,000 (Critical)   |                         |
| `hikaricp_connections_active`           | > 18 (90% pool)        | 连接池即将耗尽          |
| `system_cpu_usage`                      | > 80%                  | 系统 CPU 瓶颈           |
| `jvm_memory_used_bytes` heap            | > 80% max              | 堆内存压力              |
| `resilience4j_circuitbreaker_state`     | = 1 (OPEN)             | 熔断器已打开            |
| `ucs_cache_hit_total / miss_total`      | hit_rate < 80%         | 缓存命中率低            |

### 性能基准目标（千架无人机）

| 场景                     | 目标值               |
|--------------------------|---------------------|
| 遥测端到端延迟           | < 200ms P99         |
| Kafka 消息吞吐           | > 10,000 msg/s      |
| 后端实例数               | 3~10（HPA 自动伸缩） |
| WebSocket 推送频率       | 2Hz（500ms/帧）     |
| API 响应时间             | < 50ms P95          |
| 数据库连接池利用率       | < 80%               |

---

## 附录 A：一键启动脚本

> **推荐使用 `scripts/startup.sh`，已替代本附录中的旧脚本。**

```bash
# 全部启动（单实例模式）
./scripts/startup.sh

# 全部启动（多实例模式，Pre-K8S验证）
./scripts/startup.sh --multi-instance

# 仅启动基础设施（Kafka + Redis + DB + 监控）
./scripts/startup.sh --infra-only

# 仅启动微服务（假设基础设施已运行）
./scripts/startup.sh --services-only

# 包含 Redis Sentinel 集群
./scripts/startup.sh --with-sentinel

# 包含网关（DDS + MAVLink）
./scripts/startup.sh --with-gateways

# 停止全部服务
./scripts/startup.sh --stop
```

详细说明见 [第20节：一键启动脚本](#20-一键启动脚本)。

---

## 附录 B：Docker Compose 文件索引

| 文件                                          | 内容                                    | 依赖                          |
|-----------------------------------------------|-----------------------------------------|-------------------------------|
| `docker-compose.yml`                          | PostgreSQL + TimescaleDB                | -                             |
| `docker-compose-kafka.yml`                    | Kafka 3-Broker KRaft 集群             | -                             |
| `docker-compose-redis-cluster.yml`            | Redis 6节点集群 (3M+3S)               | -                             |
| `docker-compose-redis-sentinel.yml`           | Redis Sentinel 3节点 (自动故障转移)    | redis-cluster-net             |
| `docker-compose-monitoring.yml`               | Prometheus+Grafana+Jaeger+Nacos        | kafka-net 网络                |
| `docker-compose-microservices.yml`            | 7微服务 + Kafka + Redis + DB (单实例)  | -                             |
| `docker-compose-multi-instance-microservices.yml` | 7微服务x2 + Nginx LB (多实例验证)  | ucs-net (由microservices创建) |

---

## 附录 C：升级任务对照表 (T-41 ~ T-56) — ucs-platform 微服务架构

| 任务   | 标题                    | 关键文件（ucs-platform/）                    | 验证方式                              |
|--------|-------------------------|----------------------------------------------|--------------------------------------|
| T-41   | K8s 微服务部署          | `k8s/microservices-deployment.yaml`          | `kubectl apply --dry-run=client`     |
| T-42   | Prometheus + Grafana    | `docker-compose-monitoring.yml`              | Prometheus Targets 全 UP             |
| T-43   | Actuator + Micrometer   | 各模块 `application.yml` + `pom.xml`         | `/actuator/prometheus` 有数据        |
| T-44   | 性能指标埋点            | `ucs-telemetry-ingest/.../TelemetryMetrics.java` | Prometheus 含 `ucs_telemetry_*`  |
| T-45   | ShedLock                | `ucs-business/.../ShedLockConfig.java`       | 多实例只有1个执行定时任务            |
| T-46   | JWT RSA                 | `ucs-business/.../JwtUtil.java` + `ucs-api-gateway/.../JwtAuthGatewayFilter.java` | RSA 密钥后 JWT 使用 RS256 |
| T-47   | API 限流                | `ucs-api-gateway/.../RateLimitFilter.java`   | 超 QPS 返回 429                      |
| T-48   | 全局异常处理            | `ucs-common/.../GlobalExceptionHandler.java` | 异常返回 ApiResponse 格式            |
| T-49   | Resilience4j            | `ucs-business` + `ucs-command` application.yml | 熔断器指标可在 Grafana 查看        |
| T-50   | 命令幂等                | `ucs-command/.../IdempotencyService.java`    | 重复requestId返回409                 |
| T-51   | 分布式追踪              | 各模块 `application.yml` + Jaeger 容器       | Jaeger UI 可查看7个微服务链路        |
| T-52   | 结构化日志              | 各模块 `logback-spring.xml`                  | prod 模式输出 JSON 格式日志          |
| T-53   | Nacos 配置中心          | 各模块 `bootstrap.yml`                       | Nacos 启用后配置动态刷新             |
| T-54   | 参数校验                | `ucs-business/.../dto/ControlCommandRequest.java` | 缺失参数返回 400 + 详细错误     |
| T-55   | 统一响应 + 错误码       | `ucs-common/.../ApiResponse.java` + `ErrorCode.java` | 所有接口返回统一 JSON 格式  |
| T-56   | TLS 加密                | `scripts/tls/generate-certs.sh` + 各模块yml  | 取消注释后 SSL 握手成功              |

---

## 18. Redis Sentinel 集群（自动故障转移）

### 18.1 为什么需要 Sentinel

Redis Cluster (3M+3S) 已提供数据分片和主从复制，但 **Cluster 本身的故障转移依赖 `cluster-node-timeout`（默认 15s）**，对于高可用要求较高的场景，延迟过大。Redis Sentinel 提供：

| 功能 | 说明 |
|------|------|
| **快速故障检测** | `down-after-milliseconds=5000`（5秒判定主观下线） |
| **自动故障转移** | 多数派投票后自动提升 Slave 为新 Master |
| **客户端通知** | 通过 Pub/Sub 通知 Lettuce 客户端新 Master 地址 |
| **配置提供** | 客户端连接 Sentinel 获取当前 Master 地址（无需硬编码） |

### 18.2 架构

```
┌──────────┐  ┌──────────┐  ┌──────────┐
│Sentinel-1│  │Sentinel-2│  │Sentinel-3│
│  :26379  │  │  :26380  │  │  :26381  │
└────┬─────┘  └────┬─────┘  └────┬─────┘
     │ 监控        │ 监控        │ 监控
     ▼             ▼             ▼
┌──────────────────────────────────────┐
│       Redis Cluster (3M + 3S)       │
│ M1:6371  M2:6372  M3:6373          │
│ S1:6374  S2:6375  S3:6376          │
└──────────────────────────────────────┘
```

### 18.3 启动 Sentinel

```bash
# 前置: Redis Cluster 已启动
docker-compose -f docker-compose-redis-cluster.yml up -d
sleep 10

# 启动 Sentinel 集群
docker-compose -f docker-compose-redis-sentinel.yml up -d
```

### 18.4 验证 Sentinel

```bash
# 检查 Sentinel 状态
docker exec redis-sentinel-1 redis-cli -p 26379 sentinel masters

# 检查被监控的 Master
docker exec redis-sentinel-1 redis-cli -p 26379 sentinel get-master-addr-by-name redis-master-1

# 模拟故障转移测试
docker stop redis-node-1  # 停掉 Master-1
sleep 10
docker exec redis-sentinel-1 redis-cli -p 26379 sentinel get-master-addr-by-name redis-master-1
# 预期: 返回新的 Master 地址 (原 Slave 已被提升)
docker start redis-node-1  # 恢复，自动变为 Slave
```

### 18.5 应用层配置

每个微服务均有 `application-cluster.yml` Profile 文件，激活方式：

```bash
# 环境变量方式
export SPRING_PROFILES_ACTIVE=cluster

# 或 JVM 参数方式
java -jar ucs-business-*.jar --spring.profiles.active=cluster
```

Sentinel 配置文件位于 `redis-sentinel/sentinel-{1,2,3}.conf`。

### 18.6 Sentinel 关键参数

| 参数 | 值 | 说明 |
|------|------|------|
| `down-after-milliseconds` | 5000 | 5秒无响应判定为主观下线 |
| `failover-timeout` | 10000 | 故障转移超时时间 |
| `parallel-syncs` | 1 | 故障转移时同时同步的 Slave 数量 |
| `quorum` | 2 | 至少2个 Sentinel 同意才触发故障转移 |

---

## 19. Docker 多实例部署验证（Pre-K8S）

### 19.1 为什么在 K8S 之前用 Docker 验证

| 验证目标 | 对应机制 | 验证方法 |
|----------|----------|----------|
| 定时任务不重复执行 | ShedLock (T-45) | 查看两个实例日志，确认只有一个执行 |
| WebSocket 消息全局广播 | Redis Pub/Sub (T-27) | 连接不同实例的 WS，验证消息同步 |
| Kafka 消息负载均衡 | Consumer Group | 查看 partition 分配是否均匀 |
| 服务无状态验证 | - | 停掉任一实例，其他实例自动接管 |

### 19.2 启动多实例集群

```bash
# 1. 启动基础设施
docker-compose -f docker-compose-microservices.yml up -d

# 2. 启动多实例微服务 (每服务2个实例 + Nginx LB)
docker-compose -f docker-compose-multi-instance-microservices.yml up -d
```

### 19.3 多实例架构

```
                    ┌──────────────┐
                    │  Nginx LB    │
                    │    :80       │
                    └──────┬───────┘
                           │
                    ┌──────┴───────┐
                    │ API Gateway  │
                    │   (x1)       │
                    └──────┬───────┘
                           │
         ┌─────────────────┼─────────────────┐
         ▼                 ▼                 ▼
  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐
  │ business x2  │  │ ingest x2    │  │ store x2     │
  │ command x2   │  │ push x2      │  │ drone-state x2│
  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘
         │                 │                 │
    ┌────┴─────────────────┴─────────────────┴────┐
    │  Kafka 3-Broker  |  Redis Cluster  |  PG+TS │
    └─────────────────────────────────────────────┘
```

### 19.4 验证步骤

#### ShedLock 验证（定时任务只在1个实例执行）

```bash
docker logs ucs-business-1 2>&1 | grep -i "shedlock\|scheduled\|reconcile"
docker logs ucs-business-2 2>&1 | grep -i "shedlock\|scheduled\|reconcile"
# 预期: 只有一个实例出现 "Reconcile" 或 "scheduled task" 日志
```

#### Kafka Consumer Group 分区分配验证

```bash
docker exec ucs-kafka-1 /opt/kafka/bin/kafka-consumer-groups.sh \
  --bootstrap-server kafka-1:29092 \
  --describe --group ucs-telemetry-ingest
# 预期: partition 均匀分配到 ingest-1 和 ingest-2
```

#### 故障恢复验证

```bash
# 停掉 business-1
docker stop ucs-business-1

# 等待 30s，查看 business-2 是否接管全部定时任务
sleep 30
docker logs ucs-business-2 --since 1m 2>&1 | grep -i "shedlock\|acquired"

# 恢复 business-1
docker start ucs-business-1
```

---

## 20. 一键启动脚本

### 20.1 脚本位置

```
scripts/startup.sh
```

### 20.2 使用方式

```bash
chmod +x scripts/startup.sh

# 全部启动（单实例）
./scripts/startup.sh

# 多实例模式（Pre-K8S 验证，每服务2个实例）
./scripts/startup.sh --multi-instance

# 仅基础设施（Kafka + Redis + DB + 监控）
./scripts/startup.sh --infra-only

# 仅微服务（需基础设施已运行）
./scripts/startup.sh --services-only

# 附加选项
./scripts/startup.sh --with-sentinel    # 启用 Redis Sentinel
./scripts/startup.sh --with-gateways    # 启动 DDS/MAVLink 网关
./scripts/startup.sh --skip-build       # 跳过 Maven 构建

# 停止全部
./scripts/startup.sh --stop
```

### 20.3 启动顺序与依赖关系

```
阶段1: 基础设施
  PostgreSQL ──→ TimescaleDB ──→ Redis ──→ Kafka (3-Broker)
  └── 等待健康检查 (Kafka 可用, PG 可连接, Redis PONG)
  └── Kafka Topic 初始化

阶段1.5: Redis Sentinel (可选)
  └── 等待 3个 Sentinel 节点 PONG

阶段2: 监控栈
  Prometheus ──→ Grafana ──→ Jaeger ──→ Nacos
  └── 等待 Prometheus 和 Nacos 健康

阶段3: 构建微服务
  mvn clean package -DskipTests

阶段4: 微服务 (按依赖顺序)
  ingest ──→ store ──→ push ──→ command ──→ drone-state ──→ business
  └── 最后启动: api-gateway (依赖所有下游)
  └── 等待 Gateway /actuator/health 返回 UP

阶段5: 网关 (可选)
  DDS Gateway ──→ MAVLink Gateway

阶段6: 前端
  npm install ──→ npm run dev
```

---

## 21. 10问Q&A实施清单

以下为 `Doc/技术疑问解答_10问.md` 中各问题的建议与在 `ucs-platform` 中的实施状态：

| 问题 | 建议 | 实施状态 | 位置 |
|------|------|----------|------|
| Q1: DDS网关不需要GCS心跳 | 使用 DDS QoS Liveliness | ✅ 已实施 | `dds-gateway/dds_gateway.py` |
| Q2: 无人机上下线检测 | Redis ZSet + TTL 被动心跳 | ✅ 已实施 | `ucs-business/.../PartitionRoutingService.java` |
| Q3: Kafka offset commit | 遥测auto-commit(BATCH), ACK手动commit | ✅ 已实施 | 各模块 `application.yml` |
| Q4: Kafka lag 告警阈值 | 50K(warning) / 300K(critical) | ✅ 已实施 | `monitoring/alert-rules.yml` |
| Q5: BloomFilter 1%误判率 | 100K elements, 117KB内存 | ✅ 已实施 | `PartitionRoutingService.java` |
| Q6: reconcileDbRedis优化 | 仅查active记录 + Redis Pipeline | ✅ 已实施(T-74) | `PartitionRoutingService.java` |
| Q7: 线程池拒绝策略 | 统一使用DiscardOldestPolicy (HA优先，不阻塞Consumer) | ✅ 已实施 | `ucs-business/.../ThreadPoolConfig.java` + `ucs-command/.../ThreadPoolConfig.java` |
| Q8: 三级缓存读取顺序 | L1 Caffeine → L2 Redis → L3 DB | ✅ 已实施(T-24) | `ucs-common/.../MultiLevelCacheService.java` |
| Q9: 缓存击穿互斥锁 | mutex lock(强一致) + logical expiration | ✅ 已实施(T-24/T-25) | `MultiLevelCacheService.java` |
| Q10: ShedLock vs XXL-Job | ShedLock 适合当前规模 | ✅ 已实施(T-45) | `ucs-business/.../ShedLockConfig.java` |

---

## 12. Nacos 配置中心集成

> **说明**: Nacos 在当前架构中作为 **可选的集中配置管理** 组件。不使用 Nacos 时，微服务通过各自的 `application.yml` 和环境变量运行。K8S 部署时可用 ConfigMap 替代。

### 12.1 当前集成状态

| 组件 | 状态 | 说明 |
|------|------|------|
| `bootstrap.yml` | ✅ 全部7个微服务已配置 | 引用 Nacos 发现和配置服务 |
| `ucs-common.yml` | ✅ 共享配置已创建 | `nacos-config/ucs-common.yml` |
| Nacos Docker 容器 | ✅ 已包含在 monitoring compose | 独立部署(standalone) |

### 12.2 三种配置模式对比

| 模式 | 何时使用 | 配置方式 |
|------|----------|----------|
| **本地配置** | 开发环境 | 直接使用 `application.yml` 中的默认值 |
| **Nacos 集中配置** | 测试/预发布环境 | 启用 bootstrap.yml + Nacos |
| **K8S ConfigMap** | 生产 K8S 环境 | ConfigMap 挂载为配置文件，不需要 Nacos |

### 12.3 启用 Nacos

```bash
# 1. 启动 Nacos
docker-compose -f docker-compose-monitoring.yml up -d nacos

# 2. 导入共享配置
curl -X POST "http://localhost:8848/nacos/v1/cs/configs" \
  -d "dataId=ucs-common.yml&group=UCS_GROUP&type=yaml" \
  --data-urlencode "content@nacos-config/ucs-common.yml"

# 3. 微服务启动时激活 Nacos
export NACOS_ENABLED=true
export NACOS_SERVER_ADDR=localhost:8848
java -jar ucs-business-*.jar
```

### 12.4 Nacos 管理界面

- 地址: http://localhost:8848/nacos
- 账号: nacos / nacos
- 配置管理 → 配置列表 → `UCS_GROUP` 组下查看和编辑所有配置
