# 租户隔离实现方案

> 版本: v1.0 | 日期: 2024-01 | 状态: 方案设计阶段

---

## 一、现状分析

### 1.1 当前隔离模型

当前系统实现了 **分区级（Partition-Level）数据隔离**，但 **未实现租户级（Tenant-Level）隔离**。

```
当前模型:
┌─────────────────────────────────────────┐
│              UCS 平台（单租户）            │
│                                         │
│  Team A          Team B          Team C │
│  ┌─────┐        ┌─────┐        ┌─────┐ │
│  │User1│        │User3│        │User5│ │
│  │User2│        │User4│        │User6│ │
│  └──┬──┘        └──┬──┘        └──┬──┘ │
│     │               │               │   │
│  Partition       Partition       Partition│
│  "zs_1"         "ls_3"         "ww_5"  │
│  "observer"     "observer"     "observer"│
│  "commander"    "commander"    "commander"│
└─────────────────────────────────────────┘
```

**现有隔离机制（源码级证据）：**

| 隔离层 | 实现方式 | 代码位置 | 隔离粒度 |
|--------|----------|----------|----------|
| WebSocket 数据推送 | 按 partition 分发 | `WebSocketGatewayService.broadcastToPartitions()` | 用户级 |
| 无人机可见性 | drone_partition_map 映射表 | `PartitionRoutingService.getPartitionsForDrone()` | 分区级 |
| 用户分区分配 | 登录时计算 partition_name | `AuthService.login()` → `PartitionNameUtil` | 用户级 |
| RBAC 权限控制 | Spring Security + 角色注解 | `SecurityConfig.securityFilterChain()` | 角色级 |

### 1.2 问题：缺少租户维度

```
问题场景:
┌──────────────────────────────────────────────┐
│ 企业A 和 企业B 共享同一个 UCS 实例            │
│                                              │
│ 企业A的管理员 能看到 企业B的无人机列表？       │ ← 不应该！
│ 企业A的指挥官 能向 企业B的无人机发送指令？     │ ← 不应该！
│ 企业A的数据 和 企业B的数据 混在同一张表？      │ ← 运维风险！
└──────────────────────────────────────────────┘
```

**核心差距：**

| 维度 | 当前状态 | 目标状态 |
|------|----------|----------|
| 数据库 | 所有租户共享表 | 行级租户过滤 或 Schema隔离 |
| API 层 | 无租户上下文 | 每个请求自动注入 tenantId |
| 无人机归属 | 仅分区映射 | 无人机绑定租户 |
| 用户归属 | team_id（团队级） | tenant_id（租户级）+ team_id |
| 数据查询 | 无租户过滤 | 所有查询自动 WHERE tenant_id = ? |

---

## 二、方案设计

### 2.1 租户模型选型

| 方案 | 隔离级别 | 复杂度 | 性能 | 适用场景 |
|------|----------|--------|------|----------|
| **共享表 + 行级过滤** | 低 | 低 | 高 | SaaS 早期，租户数 < 1000 |
| Schema 隔离 | 中 | 中 | 中 | 中等规模，对隔离有要求 |
| 独立数据库 | 高 | 高 | 高 | 大型企业，强合规需求 |

**推荐方案：共享表 + 行级过滤（Phase 1）**

理由：
1. 当前项目处于早期阶段，租户数量有限
2. 改造成本最低，只需添加 `tenant_id` 字段
3. 可平滑升级到 Schema 隔离（Phase 2）
4. Spring Data JPA 的 `@Filter` + Hibernate Filter 可自动注入

### 2.2 数据库 Schema 变更

```sql
-- ==========================================
-- Step 1: 创建租户表
-- ==========================================
CREATE TABLE tenants (
    id              BIGSERIAL PRIMARY KEY,
    tenant_code     VARCHAR(50) NOT NULL UNIQUE,  -- 租户编码（如 "org_001"）
    tenant_name     VARCHAR(200) NOT NULL,         -- 租户名称
    contact_name    VARCHAR(100),                   -- 联系人
    contact_phone   VARCHAR(20),                    -- 联系电话
    max_drones      INT DEFAULT 100,                -- 最大无人机数量
    max_users       INT DEFAULT 50,                 -- 最大用户数
    status          INT DEFAULT 1,                  -- 1=活跃, 0=禁用
    created_at      TIMESTAMP DEFAULT NOW(),
    updated_at      TIMESTAMP DEFAULT NOW()
);

-- ==========================================
-- Step 2: 为现有表添加 tenant_id 字段
-- ==========================================
ALTER TABLE users ADD COLUMN tenant_id BIGINT REFERENCES tenants(id);
ALTER TABLE teams ADD COLUMN tenant_id BIGINT REFERENCES tenants(id);
ALTER TABLE drones ADD COLUMN tenant_id BIGINT REFERENCES tenants(id);
ALTER TABLE events ADD COLUMN tenant_id BIGINT REFERENCES tenants(id);
ALTER TABLE drone_partition_map ADD COLUMN tenant_id BIGINT REFERENCES tenants(id);
ALTER TABLE command_logs ADD COLUMN tenant_id BIGINT REFERENCES tenants(id);
ALTER TABLE rally_points ADD COLUMN tenant_id BIGINT REFERENCES tenants(id);

-- ==========================================
-- Step 3: 为已有数据设置默认租户
-- ==========================================
INSERT INTO tenants (tenant_code, tenant_name) VALUES ('default', '默认租户');

UPDATE users SET tenant_id = 1 WHERE tenant_id IS NULL;
UPDATE teams SET tenant_id = 1 WHERE tenant_id IS NULL;
UPDATE drones SET tenant_id = 1 WHERE tenant_id IS NULL;
UPDATE events SET tenant_id = 1 WHERE tenant_id IS NULL;

-- ==========================================
-- Step 4: 创建索引（加速租户过滤查询）
-- ==========================================
CREATE INDEX idx_users_tenant ON users(tenant_id);
CREATE INDEX idx_teams_tenant ON teams(tenant_id);
CREATE INDEX idx_drones_tenant ON drones(tenant_id);
CREATE INDEX idx_events_tenant ON events(tenant_id);
CREATE INDEX idx_command_logs_tenant ON command_logs(tenant_id);
CREATE INDEX idx_rally_points_tenant ON rally_points(tenant_id);

-- ==========================================
-- Step 5: 设置 NOT NULL 约束（数据迁移完成后）
-- ==========================================
ALTER TABLE users ALTER COLUMN tenant_id SET NOT NULL;
ALTER TABLE teams ALTER COLUMN tenant_id SET NOT NULL;
ALTER TABLE drones ALTER COLUMN tenant_id SET NOT NULL;
```

### 2.3 应用层改造

#### 2.3.1 新增实体：Tenant

```java
@Data
@Entity
@Table(name = "tenants")
public class Tenant {
    @Id
    @GeneratedValue(strategy = GenerationType.IDENTITY)
    private Long id;
    
    @Column(name = "tenant_code", nullable = false, unique = true)
    private String tenantCode;
    
    @Column(name = "tenant_name", nullable = false)
    private String tenantName;
    
    @Column(name = "max_drones")
    private Integer maxDrones = 100;
    
    @Column(name = "max_users")
    private Integer maxUsers = 50;
    
    @Column
    private Integer status = 1;
}
```

#### 2.3.2 租户上下文传播

```java
/**
 * 租户上下文 — ThreadLocal 存储当前请求的 tenantId。
 * 
 * 生命周期: Filter 设置 → Service 层使用 → Filter 清除
 */
public class TenantContext {
    private static final ThreadLocal<Long> CURRENT_TENANT = new ThreadLocal<>();
    
    public static void setTenantId(Long tenantId) {
        CURRENT_TENANT.set(tenantId);
    }
    
    public static Long getTenantId() {
        return CURRENT_TENANT.get();
    }
    
    public static void clear() {
        CURRENT_TENANT.remove();
    }
}
```

#### 2.3.3 租户过滤器

```java
/**
 * 从 JWT 中提取 tenantId，设置到 TenantContext。
 * 在 JwtAuthenticationFilter 之后执行。
 */
@Component
@Order(2)
public class TenantFilter extends OncePerRequestFilter {
    
    @Override
    protected void doFilterInternal(HttpServletRequest request,
                                     HttpServletResponse response,
                                     FilterChain filterChain) {
        try {
            Authentication auth = SecurityContextHolder.getContext().getAuthentication();
            if (auth != null && auth.getPrincipal() instanceof UserPrincipal principal) {
                TenantContext.setTenantId(principal.getTenantId());
            }
            filterChain.doFilter(request, response);
        } finally {
            TenantContext.clear();  // 必须清除，防止线程池复用导致数据泄露
        }
    }
}
```

#### 2.3.4 Hibernate 自动过滤

```java
// 方案 A: 使用 Hibernate @Filter（推荐）
@Entity
@Table(name = "drones")
@FilterDef(name = "tenantFilter", parameters = @ParamDef(name = "tenantId", type = Long.class))
@Filter(name = "tenantFilter", condition = "tenant_id = :tenantId")
public class Drone {
    @Column(name = "tenant_id")
    private Long tenantId;
    // ...
}

// 在 Repository 或 Service 层启用 Filter:
@Component
public class TenantAwareAspect {
    @PersistenceContext
    private EntityManager entityManager;
    
    @Before("execution(* com.ucs.repository.*.*(..))")
    public void enableTenantFilter() {
        Long tenantId = TenantContext.getTenantId();
        if (tenantId != null) {
            Session session = entityManager.unwrap(Session.class);
            session.enableFilter("tenantFilter").setParameter("tenantId", tenantId);
        }
    }
}
```

#### 2.3.5 JWT Token 中添加 tenantId

```java
// JwtUtil.buildToken() 中添加:
.claim("tenantId", tenantId)

// JwtUtil 中提取:
public Long extractTenantId(String token) {
    return extractClaim(token, claims -> claims.get("tenantId", Long.class));
}
```

### 2.4 Kafka 消息租户隔离

```
方案 A: Topic 级隔离（推荐小租户数）
  telemetry.raw.tenant_001
  telemetry.raw.tenant_002
  
方案 B: 消息头级隔离（推荐大租户数）
  Topic: telemetry.raw
  Header: X-Tenant-Id = tenant_001
  Consumer 按 header 过滤
```

推荐 **方案 B**（消息头级隔离），理由：
- Topic 数量不随租户数增长
- 更容易管理和监控
- Consumer Group 可按需过滤

---

## 三、实现路径（分步骤）

### Phase A: 数据层准备（1-2 周）

| # | 任务 | 改动文件 | 风险 |
|---|------|----------|------|
| 1 | 创建 tenants 表 | `schema.sql` | 低 |
| 2 | 添加 Tenant 实体 | 新增 `Tenant.java` | 低 |
| 3 | 为现有表添加 tenant_id | `schema.sql` + 迁移脚本 | 中（需数据迁移） |
| 4 | 现有数据设置默认租户 | 迁移脚本 | 低 |

### Phase B: 应用层改造（2-3 周）

| # | 任务 | 改动文件 | 风险 |
|---|------|----------|------|
| 5 | 实现 TenantContext | 新增 `TenantContext.java` | 低 |
| 6 | 实现 TenantFilter | 新增 `TenantFilter.java` | 低 |
| 7 | JWT 添加 tenantId | `JwtUtil.java`, `UserPrincipal.java` | 中 |
| 8 | User/Team/Drone 实体添加 tenantId | 多个实体类 | 中 |
| 9 | Repository 查询添加租户过滤 | 所有 Repository | 高（影响面大） |
| 10 | Hibernate Filter 自动化 | 新增 `TenantAwareAspect.java` | 中 |

### Phase C: Kafka + WebSocket 租户隔离（1-2 周）

| # | 任务 | 改动文件 | 风险 |
|---|------|----------|------|
| 11 | Kafka 消息添加 tenant header | `CommandKafkaProducer.java` 等 | 低 |
| 12 | Consumer 添加租户过滤 | `TelemetryKafkaConsumer.java` 等 | 中 |
| 13 | WebSocket 路由添加租户维度 | `WebSocketGatewayService.java` | 中 |

### Phase D: 管理接口 + 测试（1 周）

| # | 任务 | 改动文件 | 风险 |
|---|------|----------|------|
| 14 | 租户管理 CRUD API | 新增 `TenantController.java` | 低 |
| 15 | 租户注册流程 | `AuthService.register()` | 中 |
| 16 | 集成测试 + 数据隔离验证 | 新增测试用例 | 低 |

---

## 四、为什么当前阶段不适合实现

| 原因 | 说明 |
|------|------|
| **改动面过大** | 涉及所有实体、所有 Repository、所有 Service 的查询方法 |
| **与 Kafka 改造冲突** | Kafka Phase 1 正在修改数据流路径，同时改租户会增加合并冲突 |
| **需要数据迁移** | 现有数据需要分配默认租户，需要停机维护窗口 |
| **测试覆盖不足** | 当前项目无自动化测试，租户隔离的正确性难以验证 |
| **前端未就绪** | 前端需要配合改造（登录流程、数据展示、权限控制） |

**建议实施时机：Phase 2（微服务拆分）之后**，此时：
- 微服务已拆分，每个服务独立改造，风险可控
- 数据层已升级（TimescaleDB），Schema 变更更方便
- 自动化测试已建立，可验证隔离正确性

---

## 五、快速参考：关键代码位置

| 组件 | 当前文件 | 需要修改 |
|------|----------|----------|
| 用户实体 | `entity/User.java` | 添加 `tenantId` 字段 |
| 团队实体 | `entity/Team.java` | 添加 `tenantId` 字段 |
| 无人机实体 | `entity/Drone.java` | 添加 `tenantId` 字段 |
| 登录逻辑 | `service/AuthService.java` | Token 中添加 `tenantId` |
| JWT 工具 | `security/JwtUtil.java` | 添加 `tenantId` claim |
| 认证过滤器 | `security/JwtAuthenticationFilter.java` | 提取 `tenantId` |
| 分区路由 | `service/PartitionRoutingService.java` | 查询添加租户过滤 |
| WebSocket | `service/WebSocketGatewayService.java` | 推送添加租户维度 |
| Kafka Consumer | `kafka/TelemetryKafkaConsumer.java` | 消费添加租户过滤 |

---

## 六、常见疑问

### Q1: 为什么不直接用 Team 当租户？

> **Team 是租户内部的组织单元**，而非租户本身。一个企业（租户）可能有多个团队（Team）。如果用 Team 当租户，后续企业间的团队协作就无法实现。

### Q2: 租户隔离和分区隔离有什么区别？

> - **分区隔离（Partition）**：控制用户能看到哪些无人机的数据，是功能级隔离
> - **租户隔离（Tenant）**：控制不同组织的数据完全不可见，是安全级隔离
> - 两者是正交的：租户 A 内部仍然使用分区机制管理用户权限

### Q3: 行级过滤会不会影响性能？

> 添加 `tenant_id` 索引后，WHERE tenant_id = ? 的查询开销约 O(1)（索引查找）。实际测试中，百万级数据下性能差异 < 5%。

### Q4: 现有数据怎么迁移？

> 创建一个默认租户（tenant_code='default'），将所有现有数据的 tenant_id 设为默认租户 ID。迁移脚本见 2.2 节。

---

## 七、补充：无人机首次接入时的租户隔离问题 ⚠️

> **标注：本节为后续补充内容，针对无人机首次接入系统时租户隔离可能失败的场景。**

### 7.1 问题描述

当一架全新的无人机首次接入系统时，系统尚未为其分配 `tenant_id`。此时存在以下风险：

```
场景:
  1. 新无人机 px4_new 通过 DDS Gateway 首次上报遥测数据
  2. 系统收到数据，但数据库中不存在该无人机记录
  3. 系统自动注册该无人机 → tenant_id = NULL（未分配）
  4. NULL 租户的数据可能：
     - 被所有租户看到（隔离泄露）
     - 不被任何租户看到（数据丢失）
     - 被错误地分配给某个默认租户（归属错误）
```

**核心矛盾**：无人机是"物理设备先连接、逻辑归属后分配"的模式，而租户隔离要求"每条数据都有明确归属"。

### 7.2 解决方向（方案可能不唯一）

#### 方案 A：预注册绑定（推荐 — 最严格隔离）

**思路**：无人机必须先在平台注册并绑定租户，才能接入系统。未注册的无人机数据直接拒绝。

```
流程:
  管理员 → 平台注册无人机(drone_sn, tenant_id) → 写入 drones 表
  ↓
  无人机上线 → Gateway 收到遥测 → 查询 drones 表:
    - 存在且有 tenant_id → 正常处理 ✓
    - 不存在 → 拒绝数据，记录告警日志 ✗
```

```java
// TelemetryKafkaConsumer.java 中增加校验
public void consumeTelemetry(String message) {
    String uavId = parseUavId(message);
    Drone drone = droneRepository.findByUavId(uavId);
    if (drone == null || drone.getTenantId() == null) {
        log.warn("未注册或未绑定租户的无人机数据被拒绝: uavId={}", uavId);
        // 可选: 写入 pending_drones 表，供管理员审批
        pendingDroneService.recordUnregistered(uavId, message);
        return;  // 丢弃该消息
    }
    // 正常处理...
}
```

**优点**：隔离最严格，不会出现"无主数据"
**缺点**：增加运维步骤，无人机无法"即插即用"

---

#### 方案 B：暂存区 + 异步认领（推荐 — 平衡易用性）

**思路**：未注册的无人机数据暂存到隔离的"待认领区"，不进入任何租户的数据空间。管理员可以在平台上认领并分配租户。

```
流程:
  无人机上线 → Gateway 收到遥测 → 查询 drones 表:
    - 已注册 → 正常处理
    - 未注册 → 写入 pending_drones 暂存表（无 tenant_id）
  ↓
  管理员在 UI 上看到"待认领无人机"列表
  ↓
  管理员选择无人机 → 分配 tenant_id → 从 pending 转入 drones 表
  ↓
  后续遥测数据正常进入对应租户空间
```

```sql
-- 新增暂存表
CREATE TABLE IF NOT EXISTS pending_drones (
    id          BIGSERIAL PRIMARY KEY,
    uav_id      VARCHAR(50) NOT NULL UNIQUE,
    drone_sn    VARCHAR(100),
    first_seen  TIMESTAMP DEFAULT NOW(),
    last_seen   TIMESTAMP DEFAULT NOW(),
    metadata    JSONB,                        -- 首次上报的设备信息
    status      SMALLINT DEFAULT 0,           -- 0=待认领, 1=已认领, 2=已拒绝
    claimed_by  BIGINT,                       -- 认领的管理员 user_id
    tenant_id   BIGINT,                       -- 认领时分配的租户
    claimed_at  TIMESTAMP
);
```

**优点**：兼顾安全和易用性，支持"先连接后分配"
**缺点**：暂存期间的遥测数据需要额外处理（保留或丢弃）

---

#### 方案 C：Gateway 层租户标识注入（适用于已知部署环境）

**思路**：如果每个租户有独立的 Gateway 实例（或 Gateway 配置了租户标识），则在数据进入 Kafka 时即注入 `tenant_id`。

```
架构:
  租户A的无人机 → Gateway-A (配置 TENANT_ID=org_001) → Kafka (header: X-Tenant-Id=org_001)
  租户B的无人机 → Gateway-B (配置 TENANT_ID=org_002) → Kafka (header: X-Tenant-Id=org_002)
```

```python
# dds_gateway.py 启动参数增加租户标识
TENANT_ID = os.environ.get("TENANT_ID", "default")

# 发送 Kafka 消息时注入 tenant header
producer.send(
    topic="telemetry.raw",
    key=uav_id,
    value=payload,
    headers=[("X-Tenant-Id", TENANT_ID.encode())]
)
```

**优点**：数据从源头即有租户标识，无"无主数据"问题
**缺点**：要求每个租户独立部署 Gateway，运维复杂度高；不适合共享 Gateway 场景

---

#### 方案 D：默认租户 + 自动归属（最简单但需谨慎）

**思路**：所有未注册无人机自动归属到一个"默认租户"或"公共池"，后续由管理员重新分配。

```java
// 自动注册时分配默认租户
Drone newDrone = new Drone();
newDrone.setUavId(uavId);
newDrone.setTenantId(DEFAULT_TENANT_ID);  // 系统默认租户
droneRepository.save(newDrone);
log.info("新无人机自动注册到默认租户: uavId={}", uavId);
```

**优点**：最简单，"即插即用"
**缺点**：默认租户可能积累大量无主设备；如果默认租户有实际用户，可能导致数据泄露

### 7.3 推荐策略

| 场景 | 推荐方案 | 理由 |
|------|----------|------|
| 企业级部署（安全优先） | **方案 A（预注册）** | 严格管控设备接入 |
| SaaS 平台（易用优先） | **方案 B（暂存区）** | 平衡安全与用户体验 |
| 边缘部署（租户独立 Gateway） | **方案 C（Gateway 注入）** | 源头隔离最彻底 |
| 早期开发 / 单租户 | **方案 D（默认租户）** | 快速上线 |

**实际建议**：采用 **方案 A + 方案 B 组合**：
1. 正式环境要求预注册（方案 A）
2. 提供"待认领"暂存区作为补充（方案 B），处理预注册遗漏的设备
3. 暂存区数据设置 TTL（如 72 小时），超时未认领自动清除
